import './App.css';

function App() {
  return (
    <main className="app-shell">
      <section className="setup" aria-labelledby="page-title">
        <p className="eyebrow">Forms setup</p>
        <h1 id="page-title">React Forms App</h1>
        <p>
          React Hook Form, Zod, Zustand, Vitest, and React Testing Library are
          ready for the next task.
        </p>
      </section>
    </main>
  );
}

export default App;
